// Re-export from lib/database.types.ts for compatibility
import type { Json as JsonType, Database as DatabaseType } from '../lib/database.types'

export type Json = JsonType
export type Database = DatabaseType
